# Frontend Environment Configuration Template for Storage Lifecycle Optimizer
# NEVER place AWS credentials or backend secrets into frontend environment variables!
# Anything starting with VITE_ is bundle-exposed to the client browser.

# Backend Control Plane API URL
VITE_API_BASE_URL=http://localhost:8000/api/v1
