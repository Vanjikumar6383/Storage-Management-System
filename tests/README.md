# System & Integration Testing Strategy

This directory contains overall system verification configurations, end-to-end integration tests, and repository test runners.

## Testing Sub-Suites
- `backend/tests`: Unit and API integration tests for FastAPI backend services.
- `frontend`: Component and unit tests for React frontend components via Vitest.
- `worker`: Background task execution and analysis verification tests.
